{
  "name": "@brutalism/tsconfig",
  "private": true,
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "scripts": {
    "format": "prettier --write --ignore-path ../../.prettierignore --ignore-path ./.prettierignore .",
    "lint": "npm run format && eslint --cache --fix --ignore-path ../../.gitignore --ext .json ."
  },
  "dependencies": {
    "@tsconfig/node18": "^18.2.2"
  },
  "devDependencies": {
    "@brutalism/eslint-config": "workspace:*",
    "eslint": "^8.56.0"
  }
}
